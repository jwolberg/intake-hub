"""Stage: extraction.

LLM-driven, schema-validated extraction of invoice metadata and line items with
per-field confidence (PRD FR2, §7 Step 3; ARCHITECTURE.md §8). Implemented in
Phase 1 (P1-T3).
"""
