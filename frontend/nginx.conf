# Serve the SPA on Cloud Run's default container port (8080). Unknown paths fall
# back to index.html so client-side routing works.
server {
  listen 8080;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
